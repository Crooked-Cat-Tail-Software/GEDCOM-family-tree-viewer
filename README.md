# GEDCOM-family-tree-viewer
A Python and JavaScript program to read a GEDCOM file and display it as web page.

Reads a GEDCOM file and outputs an Ancestry-style interactive HTML family tree. Usage: python generate_family_tree.py *.ged or python generate_family_tree *.GED

Generates a self-contained HTML file with embedded data. The HTML includes inline CSS and JavaScript to render the family tree interactively.

